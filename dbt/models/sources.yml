version: 2

sources:
  - name: bronze
    catalog: "{{ env_var('DBT_CATALOG', 'main') }}"
    schema: "{{ env_var('DBT_SCHEMA', 'default') }}"
    tables:
      - name: customers
        description: "Raw customer data generated by Faker"
