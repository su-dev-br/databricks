# databicks
sample files trinning meterial for databricks


# databricks bundle deploy --profile dev-local --target dev
# databricks bundle deploy --profile dev-local --target local


# databricks bundle destroy --target dev