# The main pipeline for lakeflow

resources:
  pipelines:
    lakeflow_etl:
      name: lakeflow_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/lakeflow_etl"

      libraries:
        - glob:
            include: ../src/lakeflow_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
